{
  "name": "hifzhelper-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy:dev": "wrangler deploy --env development",
    "deploy:prod": "wrangler deploy --env production",
    "migrate:local": "wrangler d1 migrations apply hifzhelper-maktab1-dev --local",
    "migrate:dev": "wrangler d1 migrations apply hifzhelper-maktab1-dev --remote --env development",
    "migrate:prod": "wrangler d1 migrations apply hifzhelper-maktab1 --remote --env production"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
